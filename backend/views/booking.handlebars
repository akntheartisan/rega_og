<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Order Confirmation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        color: #333;
        background-color: #f2f2f2;
        margin: 0;
        padding: 20px;
      }
      .email-container {
        max-width: 600px;
        margin: auto;
        background-color: #ffffff;
        padding: 25px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      }
      .heading {
        color: #f6a323;
        font-size: 20px;
        margin-bottom: 15px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        font-size: 14px;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 10px;
        text-align: left;
      }
      th {
        background-color: #f0f0f0;
      }
      .summary-box {
        background-color: #f7f7f7;
        padding: 15px;
        margin-top: 20px;
        border-radius: 5px;
      }
      .summary-box h3, .summary-box h4 {
        margin: 5px 0;
        color: #000;
      }
      .footer {
        margin-top: 30px;
        font-size: 12px;
        color: #777;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <h2 class="heading">Order Confirmed — Thank you for choosing RegaScooter!</h2>

      <p>Hi {{name}},</p>
      <p>We’ve received your booking. Here are your order details:</p>

      <table>
        <thead>
          <tr>
            <th>Product Name</th>
            <th>Battery Type</th>
            <th>Color</th>
            <th>Quantity</th>
          </tr>
        </thead>
        <tbody>
          {{#each order}}
            <tr>
              <td>{{productName}}</td>
              <td>{{batteryType}}</td>
              <td>{{color}}</td>
              <td>{{quantity}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>

      <div class="summary-box">
        <h2>Transaction Summary</h2>
        <h3>Total Amount: ₹{{total}}</h3>
        <h4>Paid Amount: ₹{{paidAmount}}</h4>
        <h4>Razorpay PaymentId: {{paymentId}}</h4>
      </div>


      <div class="footer">
        If you have any questions, reply to this email or contact our support team.<br/>
        &copy; {{year}} RegaScooter. All rights reserved.
      </div>
    </div>
  </body>
</html>
